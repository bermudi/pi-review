# test/ocr-v193 — OCR-derived tests

Mirrors `src/ocr-v193`. Each file preserves the upstream `Test*` name in a comment/manifest row so omissions are searchable. No paid/network model calls. Pi interactions use scripted transcript seam (`src/ocr-v193/pi-adapter`).
